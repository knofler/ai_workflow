{
  "name": "workflow-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared-lib",
    "api-gateway",
    "auth-service",
    "workflow-service",
    "notification-service",
    "frontend"
  ],
  "scripts": {
    "build:shared": "npm --workspace shared-lib run build",
    "build": "npm run build:shared",
    "dev": "echo 'Run services individually (no orchestrated dev script yet)'",
    "lint": "echo 'Add linters later'"
  }
}
